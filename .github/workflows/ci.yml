name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Lint
        run: yarn lint

  type-check:
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Prepare
        run: yarn dev:prepare:playground

      - name: Type Check
        run: yarn test:types

  build:
    runs-on: ubuntu-latest
    needs: lint

    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Prepare
        run: yarn dev:prepare

      - name: Build
        run: yarn prepack

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          retention-days: 1

  test:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Prepare
        run: yarn dev:prepare

      - name: Test (Unit)
        run: yarn test:unit

  e2e-test:
    runs-on: ubuntu-latest
    needs: build
    strategy:
      fail-fast: false
      matrix:
        playground:
          - name: openapi
            test-file: test/e2e/playground-openapi.e2e.test.ts
          - name: swagger
            test-file: test/e2e/playground-swagger.e2e.test.ts
          - name: openapi-client
            test-file: test/e2e/playground-openapi-client.e2e.test.ts
          - name: openapi-client-v7
            test-file: test/e2e/playground-openapi-client-v7.e2e.test.ts
          - name: proto
            test-file: test/e2e/playground-proto.e2e.test.ts
          - name: proto-advanced
            test-file: test/e2e/playground-proto-advanced.e2e.test.ts
        test-mode:
          - dev
          - production

    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Prepare
        run: yarn dev:prepare

      - name: Run E2E Tests (${{ matrix.playground.name }} - ${{ matrix.test-mode }})
        # Spec File Mode (non-client) production tests are allowed to fail (known Nitro bundling issue)
        continue-on-error: ${{ matrix.test-mode == 'production' && !contains(matrix.playground.name, 'client') }}
        run: yarn vitest run ${{ matrix.playground.test-file }}
        env:
          TEST_MODE: ${{ matrix.test-mode }}

  playground:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/

      - name: Prepare
        run: yarn dev:prepare

      - name: Build playground-openapi (Spec File Mode)
        run: yarn dev:build

      - name: Build playground-swagger (Swagger 2.0 Mode)
        run: yarn dev:build:swagger

      - name: Build playground-openapi-client (Client Package Mode)
        run: yarn dev:build:openapi-client

      - name: Build playground-proto (Proto RPC Mode)
        run: yarn dev:build:proto
